@model IEnumerable<ProiectIS2.Models.Produs>

@{
    ViewData["Title"] = "Meniu Restaurante";
}

<div class="container mt-4">
    @if (TempData["SuccesCos"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show shadow-sm border-0" role="alert">
            <i class="fas fa-check-circle me-2"></i> @TempData["SuccesCos"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>🍴 Meniu Produse</h1>
        <div>
            <a asp-controller="Cos" asp-action="Index" class="btn btn-warning btn-lg shadow-sm">
                🛒 Vezi Coșul meu
            </a>
            <a asp-action="Create" class="btn btn-outline-primary ms-2">Adaugă Produs Nou</a>
        </div>
    </div>

    <table class="table table-hover align-middle shadow-sm bg-white">
        <thead class="table-dark">
            <tr>
                <th>@Html.DisplayNameFor(model => model.Nume)</th>
                <th>@Html.DisplayNameFor(model => model.Pret)</th>
                <th>@Html.DisplayNameFor(model => model.Restaurant) (ID)</th>
                <th class="text-center">Comandă</th>
                <th class="text-end">Administrare</th>
            </tr>
        </thead>
        <tbody>
@foreach (var item in Model) {
            <tr>
                <td class="fw-bold">@Html.DisplayFor(modelItem => item.Nume)</td>
                <td><span class="badge bg-light text-dark fs-6">@item.Pret lei</span></td>
                <td>@Html.DisplayFor(modelItem => item.Restaurant.Id)</td>
                <td class="text-center">
                    <a asp-controller="Cos" 
                       asp-action="AdaugaInCos" 
                       asp-route-id="@item.Id" 
                       class="btn btn-success px-4 rounded-pill shadow-sm">
                        <i class="fas fa-cart-plus"></i> Adaugă în Coș
                    </a>
                </td>
                <td class="text-end">
                    <div class="btn-group">
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-secondary">Edit</a>
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-secondary">Detalii</a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Șterge</a>
                    </div>
                </td>
            </tr>
}
        </tbody>
    </table>
</div>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">